# DEMO A：最小阵地

2026-09-25。内部可玩切片 A，不是切片 C 的完整身份展示。以基线 v1.0 和用户修订 0003 为依据；本文记录实际实现及场景工程选择，不替代完整设计。

## 游玩

Windows x64 包解压后运行 DiscreteTD.exe；同目录的 pck 和 data 文件夹必须一起保留。无需安装 Godot 或 .NET SDK。

在浅色地面或高地放置机炮，再点击开始。道路不能建造。左键选择/建造，右键或 Esc 取消工具；1 选择机炮，Delete 出售选中设施，Space 暂停，Tab 切换速度，R 重开。声音可在右上关闭。

核心持续产生金币。守住三个夜晚并清除最后残敌获胜，核心被毁失败。白天停止刷怪，已存在敌人和弹体继续运行；只复苏被毁的永久塔，不给存活塔补血、不在昼夜切换时补充电容。普通充电照常运行。

## 已实现与边界

- 方格建设、报价重验、一次性消费/退款、过期实体拒绝、重开隔离。
- 机炮、核心、行军体的小/中型变体；未定义的类型/体型组合拒绝加载。
- 实际地形高度、共享反向 BFS、连续个体运动、设施阻挡与近战攻击。
- 电容与单次射击扣能、扫掠弹体、地形遮挡、伤害与残骸；射击依赖真实模拟而非动画回调。
- 暂停、倍速、中文界面、范围与占格预览、插值绘制、程序音效。

供能是明确显示的 TestSupply，每塔独立按配置充电；不是正式电网。火炮/真 AOE、巨炮、支援军团、维修、工业网络、仪式、污染、异常、存档尚未实现。多格占用已用 2×2 规则夹具验证，本关没有可建造的多格塔。没有从参考游戏导入素材。当前美术、界面布局和操作呈现均为验证用途的占位方案，不代表最终交付的美术或交互形态；简陋不影响本轮规则验收，也不构成后续视觉设计约束。

## 工程约定

三个生产程序集：纯 C# Simulation 权威状态，Content 解析与语义校验，Godot 处理输入和表现。30Hz 固定步，单线程；倍速增加步数，积压不跳过规则。敌人共享导航场，范围和扫掠使用空间桶。实体以局代数和序号识别，容器位置不是身份。

当前每步推进场景/波次、敌人行动，更新查询索引，塔充电并推进冷却/检查开火，弹体与伤害结算，处理核心失败和阶段变化。核心同黎明致死时失败优先。该顺序是 A 的实现，不代表完整能源阶段已经接入。

本场景由准备期直接进入首夜；每夜 35 秒、日间 20 秒，共三夜。最后一夜后进入残敌清理阶段才判断胜利。这些属于场景配置而非全局规则。复苏时若敌人占据残骸，设施暂不阻挡，待占格清空后恢复阻挡；不删除敌人或凭空造成伤害。该重叠策略已测试。

唯一实验数值来源是 content/demo-a.json；没有宣称已经完成平衡。generated 是构建生成副本，不要直接修改。

## 构建

实测组合：Godot 4.7.2 stable .NET（ed1daf0bf），匹配的 4.7.2.stable.mono Windows 模板；.NET SDK 10.0.401，目标 net10.0，导出包含运行时 10.0.12。Compatibility 渲染。global.json 和 packages.lock.json 记录版本。

安装这些工具后在仓库根目录执行：

```powershell
dotnet run --project tests/simulation/DiscreteTD.Checks.csproj --configuration Release
./tools/build-demo.ps1 -GodotPath 'C:/tools/Godot_console.exe' -Export
```

可用 -DotNetPath 指定便携 dotnet.exe。省略 -Export 只编译并执行引擎装配冒烟。产物在 build/demo-a。打开 game/client/project.godot 可在编辑器运行；首次需构建 C#，构建会自动复制内容数据。纯规则运行器用无额外测试框架的可执行断言，失败返回非零；之后可按复杂度迁入测试框架。

## 已执行验证

29 项规则检查通过：原子建造/退款/多格、命令去重、重开句柄、体型与内容拒绝、共享导航、昼夜残敌/弹体保留、复苏边界、核心失败优先、射击能量与地形扫掠，以及有防御可胜/空防御失败、重复运行结果一致。项目编译零警告零错误。

Godot headless 导入和运行通过；Windows Release 导出后分别独立进行 headless 与实际图形启动，退出码 0，中文画面人工检查。启动时将 DOTNET_ROOT 指向不存在的位置验证导出自带运行时；尚未在另一台干净 Windows 机器验证。

可加 --stress 执行纯模拟探针。i7-12700K、Release、201 设施/1000 敌人、1 个共享导航场、预热 1100 步后采样 300 步：p95 3.613ms、p99 3.691ms，总分配 1,641,080 字节。这是单次 CPU 探针，不是完整游戏帧率或未来电网/AOE 性能承诺。图形启动设备为 RTX 3060 Ti，驱动 610.88，1280×840。

下一切片 B 引入火炮及其安装支撑、真实能源与维修、护盾体支援，逐项替换夹具；切片 C 再达到原设计的身份展示要求。

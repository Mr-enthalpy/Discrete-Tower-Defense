# DiscreteTD.Content

已实现程序集，引用 Simulation 定义契约；DemoLoader 校验 A 场景。以下内容哈希等完整契约仍待后续切片实现。根 content 的 JSON 是规则唯一来源；解析成强类型、稳定索引的只读定义包并计算内容哈希，失败返回文件与字段路径。

字段与校验职责见[状态与契约](../../docs/architecture/状态与契约.md)。不把显示名当技能分派键，不运行任意内容脚本；每局冻结，开发重载下一局生效。打包生成副本不可直接编辑。

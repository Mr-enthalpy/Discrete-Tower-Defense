# 贡献约定

目前以设计整理和仓库准备为主。开始前先读[文档导航](docs/README.md)与[代理工作约定](AGENTS.md)，确认任务属于哪个阶段；目录骨架不代表对应系统已经实现。

## 改动与提交

围绕一个具体问题建立短期分支，例如 `docs/energy-model` 或 `feat/minimal-combat`。提交说明解释实际变化；可使用 `docs:`、`chore:`、`feat:`、`fix:` 等简短前缀。常规改动通过PR说明问题、结果与验证，初始空仓库的基线提交直接建立主分支。

设计变更同步更新受影响的规则、试跑配置和验收，不复制一套互相漂移的说明。引擎和语言尚未选择，不为了目录完整先安装依赖、创建引擎工程或填写实现。

保留他人的修改。提交前查看差异，只暂存本次工作涉及的文件；不把本地缓存、运行日志、生成产物和凭据提交进仓库。

## 当前验证

当前没有构建命令或自动化游戏测试。文档与配置改动应检查引用路径、目录说明、规则一致性和差异格式：

```sh
git diff --check
git diff --cached --check
git status --short
```

记录实际做过的验证，不把规划中的验收写成已经通过的测试。纯文档修改无需伪造游戏测试；引擎选型后再加入可在本地和CI执行的真实检查，并同步根README。

## 文件与资产

文本使用UTF-8、LF换行和文件末尾换行，默认4空格缩进；Markdown、JSON、YAML使用2空格。遵循 [.editorconfig](.editorconfig) 和 [.gitattributes](.gitattributes)。

内容使用稳定ID，显示名与关联键分开。资产原稿与运行导出分开存放，新增外部资产时记录来源与适用许可；游戏程序、社群工具包和历史调查资料不作为本项目的隐含依赖。出现实际大型资产后再决定是否引入Git LFS，不预先把所有二进制交给额外工具。
